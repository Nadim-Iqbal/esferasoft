import 'package:flutter/material.dart';

class AppColors {
  static Color container = const Color.fromARGB(100,0,133,255);
  static Color background = Colors.black;
  static Color text = Colors.white;
  static Color buttonColour = const Color(0xff0967ae); // Only this one is used in App
}